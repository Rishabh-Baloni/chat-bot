services:
  - type: web
    name: chatbot-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd app && python main.py
    envVars:
      - key: GROK_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000